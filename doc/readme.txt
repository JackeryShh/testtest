// 渲染步骤：
// 1.初始化glfw设置
// 2.创建glfw窗口
// 3.设置回调函数，用来检测鼠标、键盘输入
// 4.初始化glew库设置
// 5.创建VBO
// 6.创建、编译顶点着色器并检查是否有错误
// 7.创建、编译片元着色器并检查是否有错误
// 8.创建着色器程序，将各个着色器结合在一起(此时某种着色器的输出是某个着色器的输入)，然后连接各个着色器；
// 
// 在游戏循环中：
// 
// 1.开启鼠标、键盘检测配置；
// 2.将模型数据与VBO绑定(glBindBuffer 和glDataBuffer)-- 复制顶点数组到缓冲(VBO)中供OpenGL使用
// 3.设置opengl链接顶点属性
// 4.使用着色器程序，glUseProgram
// 5.然后调用绘制函数,glDrawArray
// 6.交换缓存内容
